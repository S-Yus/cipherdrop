# プライバシーポリシー

最終更新日: ［YYYY-MM-DD］
運営者: ［事業者名を記載］（以下「当社」）

> **本書の位置づけ（公開前に削除すること）**
> 本書は CipherDrop の実装（本リポジトリのソースコード）に基づいて技術的な事実関係を正確に記述した
> ドラフトです。個人情報保護法・GDPR 等の準拠可否は事業の実態（運営主体、サーバー所在地、想定ユーザー層）
> に依存するため、**公開前に弁護士等の専門家によるレビューを受けてください**。特に ［　］ で示した箇所は
> 事業者情報で埋める必要があります。

---

## 1. 基本方針: ゼロ知識設計による構造的な保証

CipherDrop は、**当社が技術的にユーザーの送信内容を閲覧できない**ように設計されています。これは運用上の
約束（ポリシー）ではなく、暗号の構造そのものによる保証です。

- 暗号化・復号はすべて送信者・受信者それぞれのブラウザ内で完結します（Web Crypto API、AES-256-GCM）。
- 復号鍵は共有 URL の `#` 以降（URL フラグメント）にのみ存在します。ブラウザの仕様上、URL フラグメントは
  HTTP リクエストに含まれないため、**鍵がサーバーへ送信されることは構造的にありません**。
- 当社のサーバーが保存するのは暗号文（意味を持たないバイト列）と、復号に必要な初期化ベクトル（IV）だけです。
- 内容物の平文・ファイル名は、暗号文の内部にのみ存在します。サーバー側では一切参照できません。

したがって、当社・当社の従業員・当社のインフラを差し押さえた第三者のいずれも、送信された内容を復号する
手段を持ちません。技術的な詳細は [`docs/SECURITY_WHITEPAPER.md`](docs/SECURITY_WHITEPAPER.md) を参照してください。

## 2. 収集しない情報（ゼロログポリシー）

当社は、サービスの提供に不要な情報を収集しません。具体的には、以下を**行いません**:

- アクセスログへのリクエスト本文・クエリ文字列の記録
- IP アドレスの保存・分析・第三者提供
- Cookie の発行・利用（認証・分析・広告のいずれの目的でも）
- Google Analytics 等の外部アクセス解析ツールの導入
- ユーザーを識別・追跡するための識別子（フィンガープリンティングを含む）の生成
- メールアドレス・氏名等の個人情報の登録要求（本サービスはアカウント登録を必要としません）

アプリケーションのログに出力されるのは、固定された形式のイベント（HTTP メソッド・到達したルート名・
ステータスコード・処理時間）だけです。URL・クエリ・ヘッダー・リクエスト本文・エラーメッセージの詳細は、
ログの型として存在しないため出力できません（ログの形式はソースコードで固定されており、
`tests/security-policy.test.ts` が実装から逸脱していないことを機械的に検査しています）。

## 3. 当社が保持する情報（メタデータ）

暗号化されたデータを一時的に保存するために、以下の情報だけをサーバー上で保持します。いずれも内容物の
解釈には使えません。

| 項目 | 内容 | 保持期間 |
| --- | --- | --- |
| 暗号文・IV | 意味を持たないバイト列。復号鍵が無ければ内容は不明 | 消費（1 回の受信）または有効期限のいずれか早い方まで |
| 共有 ID | 128bit のランダムな識別子。内容物や送信者と紐づく情報ではない | 同上 |
| 種別ヒント（テキスト/ファイル） | 確認画面の表示にのみ使う、暗号化されていない分類。この値自体は認証されておらず、実際の種別は復号後にしか確定しない | 同上 |
| 暗号文のバイト数 | おおよそのデータサイズが推測できる | 同上 |
| 作成日時・有効期限 | いつ送られ、いつ消えるか | 同上 |
| 鍵確認値（任意） | 復号鍵から一方向ハッシュ（SHA-256）で導出した 32bit の短い値。コピペミスで壊れたリンクを、データを失う前に検出するための仕組みで、送信者が省略できる。この値から元の鍵を復元することはできない | 同上 |

これらのメタデータは、データが 1 回受信されるか、設定された有効期限に達した時点で**物理的に削除**されます。
削除は「取得と同時に不可分に実行」され、削除前に応答を返すことはありません。詳細は
[`README.md`](README.md#仕組み確認--消費の-2-段階) を参照してください。

## 4. サーバーの通信相手

配信経路（リバースプロキシ・CDN 等）の構成によっては、通信事業者・ホスティング事業者の設備を経由する
過程で、接続元 IP アドレス等が一時的にアクセスログに記録される場合があります。これは当社が能動的に収集・
保持する情報ではなく、経路上のインフラの一般的な動作によるものです。運用構成の詳細（配信事業者名・保持
方針）は ［事業者情報を記載］ を参照してください。

## 5. 第三者提供

当社は、上記のとおり内容物を復号する手段を持たないため、**内容物を第三者に提供することはできません**。
メタデータについても、法令に基づく開示請求等、正当な法的根拠がある場合を除き、第三者に提供しません。

## 6. お問い合わせ

本ポリシーに関するお問い合わせは ［連絡先メールアドレスを記載］ までご連絡ください。
セキュリティに関する脆弱性のご報告は [`SECURITY.md`](SECURITY.md) の手順に従ってください。

## 7. 改定

本ポリシーは、サービス内容の変更や法令改正に応じて改定することがあります。重要な変更がある場合は、
本ページの「最終更新日」を更新し、可能な範囲でサービス上に告知します。
