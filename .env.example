# docker-compose.yml が読む環境変数のひな形。`.env` にコピーして値を埋める。
# .env 自体は絶対にコミットしない（.gitignore 済み）。

# Cloudflare Zero Trust ダッシュボード → Networks → Tunnels → トンネルを作成 →
# 「Docker」の接続方法を選ぶと表示されるトークン（"eyJ..." で始まる長い文字列）。
TUNNEL_TOKEN=
